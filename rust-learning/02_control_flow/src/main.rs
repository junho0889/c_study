// ╔══════════════════════════════════════════════════════════════════════════╗
// ║       🦀 Rust 학습 커리큘럼 - 2단계: 제어 흐름 (Control Flow)          ║
// ║                                                                          ║
// ║  실행 방법:                                                              ║
// ║    1. 이 폴더로 이동: cd E:\source\Study\rust-learning\02_control_flow  ║
// ║    2. 실행: cargo run                                                    ║
// ╚══════════════════════════════════════════════════════════════════════════╝
//
// ─────────────────────────────────────────────────────────────────────────
// ■ 제어 흐름이란?
// ─────────────────────────────────────────────────────────────────────────
//
//   비유: 길 찾기
//     - 프로그램 = 집에서 학교까지 가는 길
//     - 제어 흐름 = 신호등, 갈림길, 돌아가는 길
//
//   if/else  = 신호등 (초록불이면 가고, 빨간불이면 멈추고)
//   loop     = 제자리 걷기 (멈추라고 할 때까지 계속)
//   while    = 다리가 아프면 멈추기 (조건이 거짓이 될 때까지)
//   for      = 계단 오르기 (정해진 만큼만)
//   match    = 주사위 결과 보기 (각 숫자마다 다른 행동)
//
// ─────────────────────────────────────────────────────────────────────────

fn main() {
    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 1. if / else if / else - 조건에 따라 다른 길 선택하기              │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   비유: 학교 급식
    //     - 오늘 메뉴가 피자면 → 신나게 먹기
    //     - 오늘 메뉴가 된장국이면 → 조금 먹기
    //     - 그 외에는 → 그냥 먹기
    //
    //   Rust의 if는 다른 언어와 다른 점이 있어요!
    //   → if 조건에 괄호()가 없어요! (C/Java에는 있지만 Rust는 없어요)
    //   → if는 "표현식(Expression)"이에요! 값을 반환할 수 있어요!
    //
    println!("=== if / else if / else ===");

    let 점수 = 85;

    // 기본 if/else if/else
    if 점수 >= 90 {
        println!("A등급! 훌륭해요! 🌟");
    } else if 점수 >= 80 {
        println!("B등급! 잘 했어요! 👍");
    } else if 점수 >= 70 {
        println!("C등급! 조금 더 노력해요!");
    } else if 점수 >= 60 {
        println!("D등급! 더 열심히 해야 해요!");
    } else {
        println!("F등급! 다음엔 더 잘 할 수 있어요!");
    }

    // ── if를 표현식으로 사용하기 ─────────────────────────────────────────
    //
    //   이게 Rust의 특별한 점이에요!
    //   if를 변수에 대입할 수 있어요!
    //
    //   비유: 자판기
    //     - 동전을 넣으면 음료가 나오는 것처럼
    //     - if 표현식은 조건에 따라 다른 값을 "출력"해요
    //
    let 학교오는수단 = "버스";

    // if 표현식! (각 블록의 마지막 줄에 세미콜론이 없어요 → 그 값이 반환돼요)
    let 등교시간 = if 학교오는수단 == "버스" {
        30  // 버스로 30분
    } else if 학교오는수단 == "자전거" {
        15  // 자전거로 15분
    } else {
        10  // 걸어서 10분
    };  // if 표현식 끝에 세미콜론!

    println!("{} 타면 등교 시간: {}분", 학교오는수단, 등교시간);

    // 조건 연산자들
    let 키 = 150;
    let 몸무게 = 45;

    // && = 그리고 (AND), || = 또는 (OR), ! = 아님 (NOT)
    if 키 >= 140 && 몸무게 >= 40 {
        println!("롤러코스터 탈 수 있어요!");
    }

    if 키 < 130 || 몸무게 < 30 {
        println!("어린이 놀이기구를 타세요!");
    } else {
        println!("모든 놀이기구를 탈 수 있어요!");
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 2. loop - 영원히 반복하기 (직접 멈춰야 해요!)                      │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   비유: 음악 플레이어
    //     - loop = 무한 반복 재생 버튼
    //     - break = 정지 버튼
    //     - continue = 다음 곡 버튼
    //
    //   특별한 점: loop도 값을 반환할 수 있어요! (break 뒤에 값을 써요)
    //
    println!("=== loop 반복문 ===");

    let mut 카운터 = 0;

    // loop는 break를 만날 때까지 계속 반복해요
    let 결과값 = loop {
        카운터 += 1;  // 카운터를 1씩 올려요 (카운터 = 카운터 + 1 과 같아요)

        if 카운터 == 5 {
            println!("카운터가 5가 됐어요! 반복을 멈춰요.");
            break 카운터 * 2;  // break 뒤에 값을 써서 반환할 수 있어요!
        }

        println!("카운터: {}", 카운터);
    };

    println!("loop에서 나온 값: {}", 결과값);  // 10

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 3. while - 조건이 참일 때만 반복하기                               │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   비유: 배가 부를 때까지 밥 먹기
    //     - "배가 부르지 않은 동안" 계속 밥 먹기
    //     - 배가 부르면 멈추기
    //
    println!("=== while 반복문 ===");

    let mut 숫자 = 1;

    while 숫자 <= 5 {
        println!("{}의 2배는 {}", 숫자, 숫자 * 2);
        숫자 += 1;
    }
    println!("while 반복 완료!");

    // 실용적인 예: 배열을 while로 순회하기
    let 과일들 = ["사과", "바나나", "포도", "딸기"];
    let mut 인덱스 = 0;

    println!("과일 목록:");
    while 인덱스 < 과일들.len() {
        println!("  {}번째: {}", 인덱스 + 1, 과일들[인덱스]);
        인덱스 += 1;
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 4. for - 정해진 횟수만큼 또는 컬렉션을 순회하기                    │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   비유: 출석 부르기
    //     - 선생님이 출석부(컬렉션)를 보면서 한 명씩 이름을 불러요
    //     - for 는 처음부터 끝까지 차례대로 순서를 정해서 반복해요
    //
    //   for는 while보다 훨씬 안전해요!
    //   → while은 인덱스를 잘못 쓰면 배열 범위를 벗어날 수 있어요
    //   → for는 자동으로 딱 맞게 순회해요
    //
    println!("=== for 반복문 ===");

    // 배열 순회
    let 동물들 = ["강아지", "고양이", "토끼", "햄스터"];
    println!("좋아하는 동물들:");
    for 동물 in &동물들 {  // &는 배열을 "빌려온다"는 뜻 (04_ownership에서 자세히 배워요)
        println!("  - {}", 동물);
    }

    println!();

    // 범위(Range)로 반복하기
    //   0..5  = 0, 1, 2, 3, 4 (5는 포함 안 됨)
    //   0..=5 = 0, 1, 2, 3, 4, 5 (5도 포함!)
    println!("0..5 범위 (5 미포함):");
    for i in 0..5 {
        print!("{} ", i);
    }
    println!();

    println!("0..=5 범위 (5 포함):");
    for i in 0..=5 {
        print!("{} ", i);
    }
    println!();

    // 범위를 뒤집어서 카운트다운
    println!("카운트다운!");
    for i in (1..=5).rev() {  // .rev()로 뒤집어요
        println!("{}...", i);
    }
    println!("발사! 🚀");

    println!();

    // enumerate()로 인덱스와 값을 동시에 받기
    println!("인덱스와 함께 출력:");
    for (번호, 동물) in 동물들.iter().enumerate() {
        println!("  [{}] {}", 번호, 동물);
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 5. break와 continue - 반복문 조절하기                              │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   break    = 반복문 완전히 나가기 (탈출!)
    //   continue = 이번 반복은 건너뛰고 다음으로 (스킵!)
    //
    //   비유: 마라톤
    //     - break = 다리가 부러져서 완전히 포기
    //     - continue = 돌부리에 걸렸지만 계속 달리기 (그 돌은 무시)
    //
    println!("=== break와 continue ===");

    // continue 예시: 홀수만 출력하기
    println!("1~10 중 홀수만:");
    for i in 1..=10 {
        if i % 2 == 0 {
            continue;  // 짝수면 건너뛰어요
        }
        print!("{} ", i);
    }
    println!();

    // break 예시: 특정 값 찾으면 멈추기
    let 목표 = 7;
    println!("{}를 찾을 때까지 탐색:", 목표);
    for i in 1..=20 {
        if i == 목표 {
            println!("{}를 찾았어요! 탐색 완료!", i);
            break;
        }
        println!("  {} 탐색 중...", i);
    }

    println!();

    // 중첩 반복문과 레이블(label)
    //   레이블 = 'outer: 처럼 작은따옴표로 시작하는 이름
    //   중첩 반복문에서 안쪽 반복문이 아닌 바깥 반복문을 탈출할 때 사용!
    println!("중첩 반복문에서 레이블 사용:");
    'outer: for x in 0..3 {
        for y in 0..3 {
            if x == 1 && y == 1 {
                println!("(1, 1)을 찾아서 전체 탈출!");
                break 'outer;  // 바깥 반복문을 탈출!
            }
            println!("  ({}, {})", x, y);
        }
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 6. match - 패턴 매칭 (다른 언어의 switch보다 훨씬 강력!)           │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   비유: 주사위 게임
    //     - 주사위를 굴려서 나온 숫자마다 다른 행동을 해요
    //     - 1이 나오면 앞으로 1칸, 6이 나오면 한 번 더, 등등
    //
    //   match의 특징:
    //   1. 모든 경우를 반드시 처리해야 해요! (빠진 케이스 없음)
    //   2. 표현식이에요! (값을 반환할 수 있어요)
    //   3. 패턴이 아주 강력해요! (범위, 조건 등 다양하게 쓸 수 있어요)
    //   4. _ = "그 외 모든 경우" (와일드카드)
    //
    println!("=== match 패턴 매칭 ===");

    let 주사위 = 4_u32;

    match 주사위 {
        1 => println!("앞으로 1칸!"),
        2 => println!("앞으로 2칸!"),
        3 => println!("앞으로 3칸!"),
        4 => println!("앞으로 4칸!"),
        5 => println!("앞으로 5칸!"),
        6 => println!("한 번 더 굴려요! 🎲"),
        _ => println!("이런 경우는 없어요"),  // _ 는 "나머지 모든 경우"
    }

    // 여러 패턴 한번에 처리 (| 사용)
    let 숫자2 = 3;
    match 숫자2 {
        1 | 3 | 5 | 7 | 9 => println!("{} 는 홀수!", 숫자2),
        2 | 4 | 6 | 8 | 10 => println!("{} 는 짝수!", 숫자2),
        _ => println!("{} 는 범위 밖이에요", 숫자2),
    }

    // 범위 패턴 (..= 사용)
    let 나이 = 15;
    let 나이대 = match 나이 {
        0..=6   => "유아",
        7..=12  => "초등학생",
        13..=18 => "청소년",
        19..=64 => "성인",
        65..=   => "노인",
        // _ 없어도 됨 (위 패턴들이 모든 u8 범위를 커버하니까요)
    };
    println!("{}살은 {}이에요", 나이, 나이대);

    // match에서 변수 캡처하기
    let 좌표 = (0, -2);
    match 좌표 {
        (0, 0) => println!("원점!"),
        (x, 0) => println!("x축 위의 점! x = {}", x),
        (0, y) => println!("y축 위의 점! y = {}", y),
        (x, y) => println!("일반 점! ({}, {})", x, y),
    }

    // match 가드 (조건 추가하기)
    let 온도 = 25;
    match 온도 {
        t if t < 0  => println!("{}도 - 얼어붙을 것 같아요! 🥶", t),
        t if t < 10 => println!("{}도 - 두꺼운 옷을 입어요!", t),
        t if t < 25 => println!("{}도 - 딱 좋은 날씨예요! 😊", t),
        t if t < 35 => println!("{}도 - 더워요! 아이스크림 먹어요 🍦", t),
        t           => println!("{}도 - 폭염이에요! 물 많이 마셔요! 🌡️", t),
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 7. 실전 예제: 구구단 출력                                          │
    // └────────────────────────────────────────────────────────────────────┘

    println!("=== 구구단 (3단, 4단) ===");

    for 단 in 3..=4 {
        println!("\n--- {}단 ---", 단);
        for i in 1..=9 {
            println!("{} × {} = {}", 단, i, 단 * i);
        }
    }

    println!();

    // ┌────────────────────────────────────────────────────────────────────┐
    // │ 8. 실전 예제: FizzBuzz (프로그래밍 고전 문제)                      │
    // └────────────────────────────────────────────────────────────────────┘
    //
    //   1부터 20까지 숫자를 출력하는데:
    //   - 3의 배수면 "Fizz"
    //   - 5의 배수면 "Buzz"
    //   - 3과 5의 공배수면 "FizzBuzz"
    //   - 그 외에는 숫자 그대로
    //
    println!("=== FizzBuzz (1~20) ===");

    for i in 1..=20 {
        let 출력 = match (i % 3, i % 5) {
            (0, 0) => String::from("FizzBuzz"),
            (0, _) => String::from("Fizz"),
            (_, 0) => String::from("Buzz"),
            _      => i.to_string(),
        };
        print!("{} ", 출력);
    }
    println!();

    println!();
    println!("╔══════════════════════════════════════════╗");
    println!("║  2단계 완료! 제어 흐름 마스터! 🎉        ║");
    println!("║  다음은 03_functions 단계예요!           ║");
    println!("╚══════════════════════════════════════════╝");
}
