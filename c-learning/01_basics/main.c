/*
■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■

  C 학습 01단계: 기초의 기초
  ─ 변수, 자료형, 상수, 연산자, 입출력 ─

  ■ 컴파일: gcc -std=c11 -Wall -o 01_basics main.c
  ■ 실행:   ./01_basics (Linux/Mac) 또는 01_basics.exe (Windows)

  ■ 이 파일에서 배우는 것:
      1. Hello World (printf)
      2. 변수와 자료형 (int, float, double, char)
      3. 상수 (#define 과 const)
      4. 산술 연산자
      5. Printf 형식 지정자 (%d, %f, %c, %s)
      6. Scanf 입력
      7. sizeof 연산자

■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
*/

// ─────────────────────────────────────────────────────────────────────────────
// ■ #include 란?
// ─────────────────────────────────────────────────────────────────────────────
//
//   "다른 파일에 미리 만들어둔 기능을 가져다 쓰겠다" 라는 뜻입니다.
//
//   비유: 마치 요리할 때 냉장고에서 재료를 꺼내는 것처럼,
//         프로그래밍에서는 #include 로 필요한 도구를 빌려옵니다.
//
//   <stdio.h>  → 화면에 글자를 보여주는(printf) 도구가 들어있습니다.
//                또 키보드로 입력받는(scanf) 도구도 들어있습니다.
//                "std" = standard(표준), "io" = input/output(입출력)
//
//   <stdlib.h> → 메모리 관리, 숫자↔문자열 변환 등 여러 도구가 있습니다.
//
//   <math.h>   → 제곱근(sqrt), 거듭제곱(pow) 같은 수학 도구가 있습니다.
//
//   주의: 각괄호 <> 안에 쓰는 것은 컴퓨터가 기본으로 제공하는 파일,
//         큰따옴표 "" 안에 쓰는 것은 우리가 직접 만든 파일입니다.
//
#include <stdio.h>   // printf, scanf
#include <stdlib.h>  // 여러 유틸리티 함수
#include <math.h>    // 수학 함수 (링크 시 -lm 필요)

// ─────────────────────────────────────────────────────────────────────────────
// ■ #define 으로 상수 만들기
// ─────────────────────────────────────────────────────────────────────────────
//
//   #define 은 "이 이름을 만날 때마다 이 값으로 바꿔라" 는 명령입니다.
//
//   비유: 수학 문제에서 π(파이) = 3.14159 로 약속하고 쓰는 것처럼,
//         프로그램에서도 자주 쓰는 값에 이름을 붙입니다.
//
//   관례: 상수 이름은 모두 대문자로 씁니다. (다른 변수와 구분하기 위해)
//
//   ┌─────────────────────────────────────────────────┐
//   │  #define 이름  값                                │
//   │  #define PI    3.14159                           │
//   └─────────────────────────────────────────────────┘
//
#define PI          3.14159    // 원주율
#define MAX_SIZE    100        // 배열 최대 크기
#define SCHOOL_NAME "행복초등학교"  // 학교 이름

// ─────────────────────────────────────────────────────────────────────────────
// ■ main 함수란?
// ─────────────────────────────────────────────────────────────────────────────
//
//   모든 C 프로그램은 main 에서 시작합니다.
//
//   비유: 연극에서 "막이 오른다" 는 신호처럼,
//         컴퓨터는 main() 을 만나면 "이제 프로그램 시작!" 이라고 알아채요.
//
//   int main(void)
//   ├── int   : 이 함수가 정수(숫자)를 돌려보낸다는 뜻
//   ├── main  : 함수 이름 (반드시 main 이어야 함!)
//   └── void  : 이 함수는 아무 입력도 받지 않는다는 뜻
//               (void = 비어있다 = nothing)
//
//   return 0; 의 의미:
//       0 = 프로그램이 성공적으로 끝났다
//       0 이 아닌 숫자 = 오류가 있었다
//       마치 시험에서 100점 = 완벽, 0점 = 틀렸다 처럼요!
//       (컴퓨터 세계에서는 0이 성공입니다 - 반대죠?)
//
int main(void) {

    // ─────────────────────────────────────────────────────────────────────
    // 1. Hello World - 가장 첫 번째 프로그램
    // ─────────────────────────────────────────────────────────────────────
    //
    //   printf 란?
    //   "print formatted" 의 줄임말입니다.
    //   화면에 글자나 숫자를 "형식에 맞게" 출력합니다.
    //
    //   비유: 프린터에서 종이가 나오듯,
    //         printf 는 화면(모니터)에 글자를 찍어냅니다.
    //
    //   \n 의 의미:
    //       \ (역슬래시) + n = newline(새 줄)
    //       마치 노트에서 엔터(Enter)키를 누르는 것과 같아요.
    //       \n 없이 쓰면 다음 출력이 바로 이어서 나옵니다.
    //
    printf("==============================================\n");
    printf("   안녕하세요! C 언어를 배워봅시다! \n");
    printf("==============================================\n\n");

    // ─────────────────────────────────────────────────────────────────────
    // 2. 변수(Variable)란?
    // ─────────────────────────────────────────────────────────────────────
    //
    //   변수 = 값을 저장하는 상자(박스)
    //
    //   비유: 우리집 서랍장을 생각해보세요.
    //         서랍마다 이름표(양말, 셔츠, 바지...)가 붙어 있고,
    //         그 안에 물건을 넣을 수 있죠.
    //         변수도 마찬가지! 이름이 있고, 값을 담을 수 있어요.
    //
    //   변수 만드는 방법:
    //   ┌────────────────────────────────────────────────┐
    //   │  자료형  변수이름  =  값;                       │
    //   │  int     age      =  10;                        │
    //   └────────────────────────────────────────────────┘
    //       ↑자료형     ↑이름  ↑대입연산자  ↑값   ↑세미콜론(문장 끝)
    //
    //   중요: C 언어에서는 모든 변수를 사용하기 전에 먼저 선언해야 해요.
    //         마치 서랍을 만들어 놓고 나서 물건을 넣는 것처럼요.
    //

    // ─────────────────────────────────────────────────────────────────────
    // 3. 자료형(Data Type)이란?
    // ─────────────────────────────────────────────────────────────────────
    //
    //   자료형 = 변수에 어떤 종류의 값을 넣을지 정하는 것
    //
    //   비유: 서랍에도 종류가 있죠?
    //         - 큰 서랍 = 큰 옷을 넣는 곳
    //         - 작은 서랍 = 양말이나 작은 물건을 넣는 곳
    //         변수도 숫자, 소수, 글자 등 종류에 따라 다른 자료형을 써요.
    //
    //   ┌──────────┬──────────────────────┬──────────────────────────────┐
    //   │ 자료형    │ 저장할 수 있는 값    │ 크기 (보통)                  │
    //   ├──────────┼──────────────────────┼──────────────────────────────┤
    //   │ char     │ 글자 하나            │ 1 바이트 (-128 ~ 127)        │
    //   │ int      │ 정수(소수점 없는 수) │ 4 바이트 (-21억 ~ +21억)     │
    //   │ float    │ 소수 (정밀도 낮음)   │ 4 바이트                     │
    //   │ double   │ 소수 (정밀도 높음)   │ 8 바이트                     │
    //   └──────────┴──────────────────────┴──────────────────────────────┘
    //
    //   바이트(byte)란?
    //       컴퓨터 메모리의 단위입니다.
    //       1 바이트 = 8 비트 (0과 1 여덟 개)
    //       마치 달걀 한 판(30개)처럼 정해진 단위가 있어요.
    //

    // int: 정수형 변수 (소수점 없는 숫자)
    // 비유: 사과 개수 세기 - 사과 3.5개는 없잖아요!
    int age        = 10;        // 나이
    int score      = 95;        // 점수
    int year       = 2025;      // 연도
    int temperature = -5;       // 온도 (음수도 가능!)

    // float: 단정밀도 부동소수점 (소수점 있는 숫자)
    // 비유: 키를 잴 때 "150.5cm" 처럼 소수점이 필요할 때
    // 주의: float 는 끝에 f 를 붙여야 해요!
    float height = 150.5f;      // 키 (cm)
    float weight = 45.3f;       // 몸무게 (kg)
    float pi_approx = 3.14f;    // 파이의 근삿값

    // double: 배정밀도 부동소수점 (float 보다 2배 정확)
    // 비유: 과학 실험에서 0.000001 같은 아주 작은 수를 다룰 때
    // float 는 소수점 6~7자리, double 은 15~16자리까지 정확해요
    double precise_pi = 3.14159265358979;  // 더 정확한 파이
    double distance = 384400.5;            // 지구-달 거리 (km)

    // char: 문자 하나를 저장
    // 비유: 알파벳 카드 한 장 - A, B, C 중에서 딱 하나만 담을 수 있어요
    // 중요: 작은따옴표 '' 를 사용해요! (큰따옴표 "" 가 아님!)
    char grade     = 'A';       // 성적 등급
    char initial   = 'K';       // 이름 첫 글자
    char newline_c = '\n';      // 특수 문자 (줄바꿈)

    // ─────────────────────────────────────────────────────────────────────
    // 4. const 상수 - 변하지 않는 값
    // ─────────────────────────────────────────────────────────────────────
    //
    //   const = "이 값은 절대 바꾸지 마세요!" 라는 약속 표시
    //
    //   비유: 학교 교칙처럼 한 번 정해지면 함부로 바꿀 수 없는 규칙이에요.
    //
    //   #define 과 const 의 차이:
    //   ┌────────────────┬───────────────────────────────────────────────┐
    //   │ #define PI 3.14 │ 컴파일 전에 글자를 통째로 교체 (타입 없음)   │
    //   │ const float PI  │ 진짜 변수처럼 타입이 있고 메모리에 저장됨    │
    //   └────────────────┴───────────────────────────────────────────────┘
    //
    const int MONTHS_IN_YEAR = 12;     // 1년은 12달
    const float GRAVITY = 9.8f;        // 중력가속도 (m/s²)
    const double SPEED_OF_LIGHT = 299792458.0;  // 빛의 속도 (m/s)

    // ─────────────────────────────────────────────────────────────────────
    // 5. printf 형식 지정자 (Format Specifier)
    // ─────────────────────────────────────────────────────────────────────
    //
    //   형식 지정자 = printf 에게 "이 변수를 어떤 모양으로 출력할까요?" 알려주기
    //
    //   비유: 사진을 인화할 때 "정방형으로 뽑아주세요", "4x6 사이즈로 뽑아주세요"
    //         처럼 출력 형식을 지정하는 것입니다.
    //
    //   ┌──────┬─────────────────────────────────────────────────────────┐
    //   │ %d   │ 정수 (decimal - 십진수)                                 │
    //   │ %f   │ 실수 (float/double)                                     │
    //   │ %lf  │ double 형 실수                                          │
    //   │ %c   │ 문자 하나 (character)                                   │
    //   │ %s   │ 문자열 (string)                                         │
    //   │ %e   │ 지수형 실수 (3.14e+02 같은 형식)                        │
    //   │ %x   │ 16진수 (hexadecimal)                                    │
    //   │ %o   │ 8진수 (octal)                                           │
    //   │ %p   │ 포인터 주소                                             │
    //   │ %%   │ % 기호 자체를 출력                                      │
    //   └──────┴─────────────────────────────────────────────────────────┘
    //
    printf("─────────────────────────────────────\n");
    printf("  변수 출력 예시\n");
    printf("─────────────────────────────────────\n");

    printf("나이: %d 살\n", age);          // %d = 정수
    printf("점수: %d 점\n", score);
    printf("키: %f cm\n", height);         // %f = 실수 (기본 소수점 6자리)
    printf("키: %.1f cm\n", height);       // %.1f = 소수점 1자리만
    printf("몸무게: %.2f kg\n", weight);   // %.2f = 소수점 2자리
    printf("성적: %c 등급\n", grade);      // %c = 문자 하나
    printf("학교: %s\n", SCHOOL_NAME);     // %s = 문자열
    printf("1년은 %d 달\n", MONTHS_IN_YEAR);
    printf("정확한 파이: %.10lf\n", precise_pi);  // 소수점 10자리
    printf("빛의 속도: %.0f m/s\n", SPEED_OF_LIGHT);
    printf("빛의 속도(지수): %e m/s\n", SPEED_OF_LIGHT);

    // 여러 값을 한 번에 출력하기
    printf("이름 첫 글자: %c, 나이: %d, 키: %.1f\n", initial, age, height);

    // ─────────────────────────────────────────────────────────────────────
    // 6. 산술 연산자 (Arithmetic Operators)
    // ─────────────────────────────────────────────────────────────────────
    //
    //   연산자 = 계산을 수행하는 기호
    //
    //   비유: 수학 시간에 배운 +, -, ×, ÷ 와 똑같아요!
    //         단, 곱하기는 *, 나누기는 / 를 씁니다.
    //
    //   ┌──────┬──────────────────────────────────────────────────────────┐
    //   │  +   │ 덧셈  : 5 + 3 = 8                                       │
    //   │  -   │ 뺄셈  : 5 - 3 = 2                                       │
    //   │  *   │ 곱셈  : 5 * 3 = 15                                      │
    //   │  /   │ 나눗셈: 5 / 3 = 1  (정수끼리 나누면 나머지 버림!)       │
    //   │  %   │ 나머지: 5 % 3 = 2  (모듈로 연산)                        │
    //   └──────┴──────────────────────────────────────────────────────────┘
    //
    //   ⚠️ 함정: 정수 나눗셈
    //      int 나누기 int = int (소수점 버려짐!)
    //      7 / 2 = 3  (3.5 가 아님!)
    //      double 로 바꾸려면: (double)7 / 2 = 3.5
    //

    printf("\n─────────────────────────────────────\n");
    printf("  산술 연산자 예시\n");
    printf("─────────────────────────────────────\n");

    int a = 17, b = 5;
    printf("a = %d, b = %d\n", a, b);
    printf("a + b = %d\n", a + b);     // 22
    printf("a - b = %d\n", a - b);     // 12
    printf("a * b = %d\n", a * b);     // 85
    printf("a / b = %d\n", a / b);     // 3 (소수점 버림!)
    printf("a %% b = %d\n", a % b);    // 2 (나머지, %% = % 출력)

    // 정수 나눗셈의 함정과 해결법
    printf("\n[정수 나눗셈 주의!]\n");
    printf("7 / 2 = %d (정수로 나누면 소수점 없어짐)\n", 7 / 2);
    printf("7.0 / 2 = %.1f (실수로 나누면 OK)\n", 7.0 / 2);
    printf("(double)7 / 2 = %.1f (형변환 사용)\n", (double)7 / 2);

    // 나머지(%) 연산의 활용
    printf("\n[나머지 연산 활용]\n");
    printf("짝수/홀수 판별: %d는 ", age);
    if (age % 2 == 0)
        printf("짝수\n");
    else
        printf("홀수\n");
    printf("17을 3으로 나눈 나머지: %d\n", 17 % 3);  // 2

    // 복합 대입 연산자
    // 비유: "과자를 3개 더 먹었다" = 과자 개수 = 과자 개수 + 3
    printf("\n[복합 대입 연산자]\n");
    int count = 10;
    printf("처음 count = %d\n", count);
    count += 5;   // count = count + 5
    printf("count += 5 → %d\n", count);
    count -= 3;   // count = count - 3
    printf("count -= 3 → %d\n", count);
    count *= 2;   // count = count * 2
    printf("count *= 2 → %d\n", count);
    count /= 4;   // count = count / 4
    printf("count /= 4 → %d\n", count);

    // 증가/감소 연산자 (++ / --)
    // 비유: 계단을 한 칸 올라가는(++) 것, 한 칸 내려오는(--) 것
    printf("\n[증가/감소 연산자]\n");
    int x = 5;
    printf("x = %d\n", x);
    printf("x++ = %d (출력 후 증가: 먼저 쓰고 나서 1 늘림)\n", x++);
    printf("x = %d (이제 6이 됨)\n", x);
    printf("++x = %d (증가 후 출력: 먼저 1 늘리고 나서 씀)\n", ++x);

    // ─────────────────────────────────────────────────────────────────────
    // 7. sizeof 연산자
    // ─────────────────────────────────────────────────────────────────────
    //
    //   sizeof = "이 자료형(또는 변수)은 메모리를 몇 바이트 차지하나요?"
    //
    //   비유: 상자가 몇 칸짜리인지 확인하는 것처럼,
    //         변수가 컴퓨터 메모리를 얼마나 쓰는지 알려줍니다.
    //
    //   ⚠️ 주의: 컴퓨터(운영체제/컴파일러)마다 크기가 다를 수 있어요!
    //            아래 결과는 64비트 시스템 기준입니다.
    //
    printf("\n─────────────────────────────────────\n");
    printf("  sizeof 연산자 - 메모리 크기 확인\n");
    printf("─────────────────────────────────────\n");

    printf("char   크기: %zu 바이트\n", sizeof(char));      // 1
    printf("int    크기: %zu 바이트\n", sizeof(int));       // 4
    printf("float  크기: %zu 바이트\n", sizeof(float));     // 4
    printf("double 크기: %zu 바이트\n", sizeof(double));    // 8
    printf("long   크기: %zu 바이트\n", sizeof(long));      // 8 (64bit)

    // 변수에도 sizeof 사용 가능
    printf("age 변수 크기: %zu 바이트\n", sizeof(age));
    printf("precise_pi 변수 크기: %zu 바이트\n", sizeof(precise_pi));

    // ─────────────────────────────────────────────────────────────────────
    // 8. scanf - 키보드로 입력 받기
    // ─────────────────────────────────────────────────────────────────────
    //
    //   scanf = "scan formatted" 의 줄임말
    //   키보드에서 입력받아 변수에 저장합니다.
    //
    //   비유: 선생님이 학생들에게 "이름을 빈 칸에 써주세요" 하는 것처럼,
    //         scanf 는 사용자에게 "값을 입력해 주세요" 합니다.
    //
    //   ⚠️ 중요: scanf 에서는 변수 앞에 & 를 붙여야 해요!
    //            & 는 "이 변수가 저장된 메모리 주소" 를 의미합니다.
    //            마치 "값을 어느 주소에 보낼까요?" 라고 묻는 것과 같아요.
    //            (포인터에서 자세히 배웁니다!)
    //
    //   scanf("%d", &변수명);   // 정수 입력
    //   scanf("%f", &변수명);   // float 입력
    //   scanf("%lf", &변수명);  // double 입력
    //   scanf("%c", &변수명);   // 문자 입력
    //   scanf("%s", 배열명);    // 문자열 입력 (&없이! 배열은 주소 자체)
    //

    printf("\n─────────────────────────────────────\n");
    printf("  scanf 입력 예시\n");
    printf("─────────────────────────────────────\n");

    int user_age;
    float user_height;

    printf("나이를 입력하세요: ");
    scanf("%d", &user_age);           // & 필수!

    printf("키(cm)를 입력하세요: ");
    scanf("%f", &user_height);        // float 는 %f

    printf("\n입력하신 정보:\n");
    printf("  나이: %d 살\n", user_age);
    printf("  키: %.1f cm\n", user_height);

    // BMI 계산 (키를 미터로 변환 후 계산)
    // BMI = 몸무게(kg) / 키(m)^2
    float height_m = user_height / 100.0f;  // cm → m 변환
    // (weight 는 위에서 선언했으나, 여기서는 예시용으로 다시 사용)
    printf("\n(참고) 파이 * 반지름^2 = 원의 넓이\n");
    printf("반지름 5인 원의 넓이: %.2f\n", PI * 5.0f * 5.0f);

    // ─────────────────────────────────────────────────────────────────────
    // 9. 형 변환 (Type Casting)
    // ─────────────────────────────────────────────────────────────────────
    //
    //   형 변환 = 한 자료형을 다른 자료형으로 바꾸기
    //
    //   비유: 달러(Dollar)를 원(Won)으로 환전하는 것처럼,
    //         int 를 double 로 바꿀 수 있어요.
    //
    //   두 가지 방법:
    //   1. 자동 형 변환 (묵시적): 컴퓨터가 알아서 해줌
    //      int + double → double 로 자동 변환
    //
    //   2. 강제 형 변환 (명시적): 우리가 직접 지정
    //      (double)정수변수 → 강제로 double 로 변환
    //
    printf("\n─────────────────────────────────────\n");
    printf("  형 변환 예시\n");
    printf("─────────────────────────────────────\n");

    int num1 = 7, num2 = 2;
    printf("정수 나눗셈: %d / %d = %d\n", num1, num2, num1 / num2);
    printf("강제 형변환: (double)%d / %d = %.1f\n",
           num1, num2, (double)num1 / num2);

    // 자동 형 변환 예시
    int i_val = 65;
    char c_val = (char)i_val;  // 65 → 'A' (ASCII 코드)
    printf("정수 %d 는 문자 '%c' (ASCII 코드)\n", i_val, c_val);

    // ─────────────────────────────────────────────────────────────────────
    // 10. 흔한 실수와 주의사항
    // ─────────────────────────────────────────────────────────────────────
    //
    //   ❌ 실수 1: 변수 초기화 안 하기
    //      int x;
    //      printf("%d", x);  // x 에 쓰레기값이 들어있어 예측 불가!
    //      → 항상 선언할 때 초기값을 넣어주세요: int x = 0;
    //
    //   ❌ 실수 2: scanf 에서 & 빠뜨리기
    //      scanf("%d", age);   // 틀림! (프로그램 충돌할 수 있음)
    //      scanf("%d", &age);  // 맞음!
    //
    //   ❌ 실수 3: float 에 f 안 붙이기
    //      float f = 3.14;    // 경고 발생 (3.14 는 double 로 인식됨)
    //      float f = 3.14f;   // 올바름
    //
    //   ❌ 실수 4: 정수 오버플로우
    //      int 의 최댓값은 약 21억입니다.
    //      int x = 2147483647;
    //      x++;  // 오버플로우! 갑자기 매우 작은 음수가 됨
    //            (마치 자동차 계기판이 999999km 넘으면 000000 되듯이)
    //

    printf("\n─────────────────────────────────────\n");
    printf("  오버플로우 시연\n");
    printf("─────────────────────────────────────\n");

    int max_int = 2147483647;  // int 의 최댓값
    printf("int 최댓값: %d\n", max_int);
    printf("최댓값 + 1: %d (오버플로우!)\n", max_int + 1);  // 음수가 됨!

    // ─────────────────────────────────────────────────────────────────────
    // 11. 연습 문제
    // ─────────────────────────────────────────────────────────────────────
    //
    //   🎯 직접 해보기:
    //
    //   1. 반지름을 입력받아 원의 넓이와 둘레를 계산하는 프로그램 만들기
    //      공식: 넓이 = PI * r * r
    //            둘레 = 2 * PI * r
    //
    //   2. 섭씨 온도를 입력받아 화씨로 변환하기
    //      공식: 화씨 = 섭씨 * 9 / 5 + 32
    //
    //   3. 세 과목의 점수를 입력받아 평균 구하기
    //      주의: int 로 계산하면 소수점이 버려짐!
    //            (double) 형변환을 쓰거나 1.0을 곱해보세요.
    //
    //   4. 1~100 사이의 숫자를 입력받아 홀수/짝수 판별하기
    //      힌트: 2로 나눈 나머지가 0이면 짝수
    //

    printf("\n==============================================\n");
    printf("  01단계 학습 완료!\n");
    printf("  다음 단계: 02_control_flow (조건문과 반복문)\n");
    printf("==============================================\n");

    return 0;  // 프로그램 성공 종료
}
